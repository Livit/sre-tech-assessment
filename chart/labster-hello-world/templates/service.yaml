apiVersion: v1
kind: Service
metadata:
  name: {{ include "labster-hello.fullname" . }}
  labels:
    {{- include "labster-hello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "labster-hello.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "labster-hello.fullname" . }}-test-connection"
  labels:
    {{- include "labster-hello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "labster-hello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
